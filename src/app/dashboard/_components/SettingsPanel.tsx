'use client'
import { useLanguage } from '../../../components/LanguageProvider'
interface SettingsPanelProps { collapseSettings: boolean; setCollapseSettings: (v: boolean) => void; homeStation: string; setHomeStation: (v: string) => void; officeStation: string; setOfficeStation: (v: string) => void; morningStart: string; setMorningStart: (v: string) => void; morningEnd: string; setMorningEnd: (v: string) => void; eveningStart: string; setEveningStart: (v: string) => void; eveningEnd: string; setEveningEnd: (v: string) => void; handleSaveSettings: () => void }
export function SettingsPanel(p: SettingsPanelProps) { const { t } = useLanguage(); const fields = [[t.homeStations,p.homeStation,p.setHomeStation,'text'],[t.officeStations,p.officeStation,p.setOfficeStation,'text'],[t.morningStart,p.morningStart,p.setMorningStart,'number'],[t.morningEnd,p.morningEnd,p.setMorningEnd,'number'],[t.eveningStart,p.eveningStart,p.setEveningStart,'number'],[t.eveningEnd,p.eveningEnd,p.setEveningEnd,'number']] as const; return <section className="space-y-3 rounded-2xl border border-slate-100 bg-white p-3.5 shadow-sm md:p-6"><div className="flex items-center gap-2"><button onClick={() => p.setCollapseSettings(!p.collapseSettings)} title={p.collapseSettings ? t.expand : t.collapse} className="flex h-6 w-6 items-center justify-center rounded-lg bg-slate-100 text-xs font-bold text-slate-600">{p.collapseSettings ? '+' : '−'}</button><h2 className="text-base font-semibold text-slate-800 md:text-lg">⚙️ {t.commuteRules}</h2></div>{!p.collapseSettings && <div className="space-y-4 border-t border-slate-100 pt-4"><div className="grid grid-cols-1 gap-3 md:grid-cols-2">{fields.slice(0,2).map(([label,value,setValue,type]) => <label key={label} className="text-xs font-medium text-slate-600">{label}<input type={type} value={value} onChange={e => setValue(e.target.value)} className="mt-1 w-full rounded-xl border border-slate-200 px-3 py-2 font-normal focus:border-sky-500 focus:outline-none" /></label>)}</div><div className="grid grid-cols-2 gap-3 md:grid-cols-4">{fields.slice(2).map(([label,value,setValue,type]) => <label key={label} className="text-xs font-medium text-slate-600">{label}<input type={type} min="0" max="23" value={value} onChange={e => setValue(e.target.value)} className="mt-1 w-full rounded-xl border border-slate-200 px-3 py-2 font-normal" /></label>)}</div><div className="flex justify-end"><button onClick={p.handleSaveSettings} className="rounded-xl bg-slate-800 px-4 py-2 text-xs font-medium text-white hover:bg-slate-900">{t.saveSettings}</button></div></div>}</section> }
