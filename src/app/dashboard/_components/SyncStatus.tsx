'use client'
import { useLanguage } from '../../../components/LanguageProvider'
interface SyncStatusProps { collapseSync: boolean; setCollapseSync: (v: boolean) => void; isConnected: boolean; stravaAuthUrl: string; handleSync: () => void; handleFullResync: () => void; isSyncing: boolean }
export function SyncStatus(p: SyncStatusProps) { const { t } = useLanguage(); return <section className="space-y-3 rounded-2xl border border-slate-100 bg-white p-3.5 shadow-sm md:p-6"><div className="flex items-center justify-between gap-2"><div className="flex items-center gap-2"><button onClick={() => p.setCollapseSync(!p.collapseSync)} title={p.collapseSync ? t.expand : t.collapse} className="flex h-6 w-6 items-center justify-center rounded-lg bg-slate-100 text-xs font-bold text-slate-600">{p.collapseSync ? '+' : '−'}</button><h2 className="text-base font-semibold text-slate-800 md:text-lg">{t.syncTitle}</h2></div>{!p.collapseSync && <div className="flex flex-wrap justify-end gap-2">{p.isConnected ? <span className="rounded-xl border border-green-200 bg-green-50 px-3 py-1.5 text-xs font-medium text-green-700">✓ {t.connected}</span> : <a href={p.stravaAuthUrl} className="rounded-xl bg-[#FC4C02] px-3 py-1.5 text-xs font-medium text-white">{t.connectStrava}</a>}<button onClick={p.handleSync} disabled={p.isSyncing} className="rounded-xl bg-sky-600 px-3 py-1.5 text-xs font-medium text-white disabled:opacity-50">{p.isSyncing ? t.syncing : t.syncData}</button><button onClick={p.handleFullResync} disabled={p.isSyncing} className="rounded-xl bg-violet-600 px-3 py-1.5 text-xs font-medium text-white disabled:opacity-50">{p.isSyncing ? t.recalculating : t.fullResync}</button></div>}</div>{!p.collapseSync && <p className="text-xs text-slate-500 md:text-sm">{t.syncDesc}</p>}</section> }
